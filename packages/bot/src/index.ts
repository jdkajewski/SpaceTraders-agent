/** @st/bot — stub (Wave 2 will fill in the bot runtime). */
import type { Config } from '@st/shared';

// Ensure @st/shared is a compile-time dependency.
const _typeCheck: Config | undefined = undefined;
void _typeCheck;

export { };
