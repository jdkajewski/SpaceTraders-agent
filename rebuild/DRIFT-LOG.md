# Drift Log — legacy behavior vs. TS port

Append-only log used by sub-sessions during Waves 2–5. When porting a function, if you find a behavior
that looks like a bug, an inconsistency with the docs, or a quirk worth a decision, **record it here
instead of silently changing it.** Resolved in Wave 6.

**Status (end of Wave 6): all entries `resolved`.** #6 was intentionally dropped (never used). Entries
#1–#5 were planning-era drifts implemented across W0–W5; #7–#35 were logged during the wave ports; #36
was discovered and fixed in W6. Behavioural fixes applied in W6: #26 (TRADE_FIRST non-mutating peek +
restored skip-contract gate, regression-tested), #30 (drop MINE_BATCH from the reserved roster), #33
(drop vestigial `getShipyards`), #36 (mineFeed `good`/`transport` snapshot parity). Comment/doc fixes:
#2, #27. The remainder are preserved-as-designed with rationale (#16, #19, #23, #24, #29) or resolved
with a pointer to the implementing wave/file (#1, #3, #4, #5, #11).

| # | Wave | Location (legacy) | Observation | Proposed action | Status |
|---|------|-------------------|-------------|-----------------|--------|
| 1 | plan | docs vs code | Docs describe ~2404-line bot; live is 3206. FLEET_SCALE, AUTO_EXPAND/expansion.mjs, REPAIR, mineMigrateManager, ship-purchase, TRADE_FIRST, CONTRACTS switch are in code but thin/absent in docs. | Port all (parity); refresh docs in W6. **W6 resolved:** all listed features ported across W0–W5 (FLEET_SCALE/ship-purchase in budget+main, expansion in `expansion/`, REPAIR+mineMigrate in `mining/`, TRADE_FIRST in `contracts/`, CONTRACTS in `contracts/`). Docs refreshed: `07-doc-drift.md` updated + new `09-ts-rebuild.md` describes the TS monorepo (docs 01–08 still describe the legacy `.mjs`). | resolved |
| 2 | plan | trade.mjs jump() | `jump()` already implemented; doc 08 lists it as TODO. | Port as-is; correct doc. **W6 resolved:** `jump` is implemented in `trade/shipActions.ts`; corrected `docs/08-expansion.md` (the "primitive to build" line now points at the implemented `jump()`). | resolved |
| 3 | plan | config | Code defaults differ from operator live-launch values. | Keep code defaults; ship live profile as example env_file. **W6 resolved:** code defaults preserved in `@st/shared` config (parity with bot2 declaration-time defaults); the operator live profile ships as `deploy/bot.env.example` (documented in `packages/bot/README.md`). | resolved |
| 4 | plan | gate-levers.json | File-mtime hot-reload of credit band. | Re-implement as polled GET /gate-levers. **W6 resolved:** implemented — API `GET/PUT /gate-levers` (W1) + bot polls it on the `targetWatch` cadence into `state.gateLevers` (W3 `budget/phase.ts`); no file-mtime watch. | resolved |
| 5 | plan | intents/run-stats | Local-file crash safety would regress behind the API. | Local write-through fallback + boot reconcile. **W6 resolved:** implemented in `recovery.ts` (`FileLocalStore` atomic write-through + `reconcileLocalToApi` newest-wins) and `clients/persistence.ts` (local-first then best-effort API, read-fallback to local). Now covered by the durable `__tests__/crash-safety.test.ts` (down@save / down@boot / reconnect reconcile). | resolved |
| 7 | W0 | bot2.mjs line 194 | `GATE_CREDIT_RESUME` default is `GATE_CREDIT_FLOOR + GATE_CREDIT_RESUME_GAP` (derived at runtime), but env override is also honoured. Zod schema can't express cross-field defaults in a single pass. | Two-step parse in `loadConfig()`: raw schema captures scalar fields; post-transform fills derived fields. `Config` type uses `Omit` to replace the raw `string \| undefined` with `number`. | resolved |
| 8 | W0 | bot2.mjs line 231-232 | `FEED_PRICE_SETTLE_MS` / `FEED_PRICE_REBOUND_EPS` defaults silently mirror the GATE counterparts at declaration time. If GATE is overridden via env, FEED picks up that value unless its own env is also set. Same cross-field derivation pattern as #7. | Same post-transform approach: FEED mirrors GATE unless FEED env var is explicitly present. Behaviour parity confirmed by tests. | resolved |
| 9 | W0 | bot2.mjs line 68 | `CONTRACT_RIDEALONG` default is `MULTI_GOOD && env.CONTRACT_RIDEALONG !== '0'` — it's off when MULTI_GOOD is off, even if CONTRACT_RIDEALONG env is unset. Zod can't express this dependency in a flat schema. | Same post-transform: raw schema parses CONTRACT_RIDEALONG as a standalone bool; loadConfig() re-derives it coupling to MULTI_GOOD. Parity confirmed by tests. | resolved |
| 10 | W0 | bot2.mjs line 1895 | `MINE_ORE_RESERVE` default is `REFINE_IN` (the JS const = 30), not a numeric literal. Anyone reading the env line without the constant context would see `|| REFINE_IN` and not know it's 30. | Hardcoded to 30 (= REFINE_IN) in the zod schema default. REFINE_IN constant exported from `@st/shared/constants.ts` so later ports can reference it. | resolved |
| 11 | W0 | bot2.mjs line 194 | `let GATE_CREDIT_FLOOR` and `let GATE_CREDIT_RESUME` are declared with `let` not `const` — they're mutated live by `reloadGateLevers()` reading gate-levers.json. In the TS port these will be read-only from `Config` and the live values come from polling `GET /gate-levers` (DRIFT entry #4). | No action in W0; noted for Wave 1 (gate-levers endpoint) and Wave 3 (gate.ts). **W6 resolved:** the live values are now `state.gateLevers.{floor,resume}` (a plain object field), seeded from `Config` and refreshed by the polled `GET /gate-levers` (DRIFT #4). No module-global `let`; `gateCreditOk`/`gateSupplyActive` read the object. Pointer: W3 `budget/phase.ts` + `runtime/state.ts`. | resolved |
| 12 | W1 | Fastify v5 + TypeScript strict | `exactOptionalPropertyTypes: true` caused friction with Prisma's `NullableJsonNullValueInput` (for `Intent.extras`) and conditional `where` clauses. The Prisma generated type `JsonValue | null` cannot be assigned to TypeBox's optional `Record<String,Unknown>` without an explicit null-strip helper. | Added `serializeIntent()` helper that strips null extras; use `Prisma.JsonNull` explicitly in upsert inputs. Note for Wave 2: the bot persistence client should use the shared DTOs (`@st/shared`) not Prisma types directly. | resolved |
| 13 | W1 | Prisma output path | Generated Prisma client lives in `src/generated/prisma/` but compiled dist imports `dist/generated/prisma/`. TypeScript's `tsc -b` does not copy non-TS files from `src/` to `dist/`. | Added `cp -r src/generated dist/generated` to the `build` script. Dockerfile updated to copy from `dist/` only. | resolved |
| 14 | W1 | API config scope | `@st/shared` config schema only had bot2.mjs fields. The API needs `API_PORT`, `API_HOST`, `BOT_KEY`, `BOT_AUTH_ENABLED`. | Appended four new fields to `RawConfigSchema` under `# API server` comment. Additive; no existing fields changed. | resolved |
| 15 | W1-merge | api routes (markets, market-history, mine-events, trade-observations) | `tsc -b` (build mode, uses shared `dist/*.d.ts`) compiled clean, but `tsc --noEmit -p` (the `typecheck` script, resolves `@st/shared` via source `paths`) reported TS7006 implicit-any on the `findMany().map((r) => ...)` callbacks — the `fastify.prisma` augmentation inference differs between the two resolution modes. Also required `prisma generate` (client is gitignored) before any build in a fresh worktree. | Annotated the four map callbacks with explicit Prisma row types (`MarketSnapshot`/`MarketHistory`/`MineEvent`/`TradeObservation`) so both `build` and `typecheck` pass. Generated the Prisma client in the integration worktree. | resolved |
| 16 | W2 | bot2.mjs L2206-2228 (fuel-nodes.json) | Legacy persists the sticky fuel-node set to `fuel-nodes.json` and seeds it from `markets.json` STRUCTURE at module load, so the router has a complete fuel graph on the very first resume before any ship re-settles. Wave 2 forbids `fs`. | Sticky fuel-node set is **in-memory only** in `routing/route.ts`; re-seeded from the API markets snapshot via `seedFuelNodes()`/`loadSnapshot()` on boot, and grows as markets refresh. Behaviour parity once the boot snapshot is loaded; the cross-restart disk durability is dropped (acceptable — the API snapshot is the new source of truth). **W6 resolved — preserved as designed:** the API markets snapshot is the canonical fuel graph (re-seeded each boot before workers start), so the legacy `fuel-nodes.json` disk durability is intentionally not ported; honours the bot's no-`fs` rule. No behavioural gap once the boot snapshot loads. | resolved |
| 17 | W2 | st.mjs / config | The SpaceTraders player token, the persistence API base URL, and the `x-bot-key` shared secret are NOT part of `@st/shared` `Config` (bot2/st read them from `process.env` directly). | **Resolved in W3:** added `API_BASE_URL` + `SPACETRADERS_PLAYER_AGENT_TOKEN` to the shared zod `Config` (additive; `BOT_KEY`/`API_HOST`/`API_PORT` landed in W1 as #14). `clients/spacetraders.ts` + `clients/persistence.ts` now take these via options only (no direct `process.env` reads); `main.ts` sources them from `cfg`. `loadConfig(process.env)` is the single env entry point. (Orchestrator referenced this as "#13"; in this log #13 is the Prisma-path entry.) | resolved |
| 18 | W2 | bot2.mjs L318-324 `computeFuelPx` | Legacy reads/writes the module-global `FUEL_PX` (default 0.72) as both input fallback and output. | Ported as a pure `computeFuelPx(markets, prior)` returning the new value; the markets service owns the live `FUEL_PX` state and injects it into routing (`chooseMode`/`routeCost` take it as a param). No module globals. Behaviour parity. | resolved |
| 19 | W2 | bot2.mjs L349-364 `getMarkets` | Legacy refreshes markets over `MARKET_WPS = Object.keys(markets.json)` and fetches each via the hardcoded `SYSTEM`. | TS `market/markets.ts` seeds the waypoint list from the API boot snapshot keys (`loadSnapshot`) and derives the system per-waypoint (`sysOf(wp)`) so multi-system markets work; identical for home-system waypoints. markets.json write → `persistence.putMarkets`; market-history.jsonl append → `persistence.appendMarketHistory`. **W6 resolved — preserved (improvement):** per-waypoint `sysOf` is a strict superset of the legacy single-`SYSTEM` refresh and is byte-identical for home-system waypoints (the only case legacy supported). Kept. | resolved |
| 20 | W3 | shared `StatusSnapshot` vs API `POST /status` body | The shared `StatusSnapshot` type (ts/ships/gate:GateState…) did not match the Wave-1 `/status` request body, which is `{phase, runNet, credits, gate?:string\|null, data:unknown}`. Only consumer is `postStatus`. | Redefined shared `StatusSnapshot` to the API body shape; the full legacy `bot-status.json`-shaped snapshot now rides inside `data` (typed `unknown`). Additive-ish: changed one shared type, flagged to orchestrator. `postStatus` signature unchanged. | resolved |
| 21 | W3 | bot2.mjs `saveIntent` extras | Legacy `Intent.extras` is an ARRAY of ride-along plans; the shared `Intent.extras` type is `Record<string,unknown>`. | Wrapped as `extras:{ rideAlongs:[...] }` in `recovery.ts` `saveIntent`; `reconcileHeldCargo` reads `extras.rideAlongs` back to replay the fill. No shared type change. | resolved |
| 22 | W3 | bot2.mjs STOP file | Legacy polls a `STOP` file on disk for graceful shutdown; Wave-3 forbids `fs` for control state. | `installStopHandlers(state)` wires SIGTERM/SIGINT to set `state.stop` (graceful drain: workers finish the in-flight action then exit). An optional polled flag is retained behind `STOP_POLL=1` (env-gated) for environments that can't signal. Drain semantics identical. | resolved |
| 23 | W3 | bot2.mjs `DYNAMIC_TARGET` | Legacy computes `DYNAMIC_TARGET = !process.env.CREDIT_TARGET` (string-presence test). Ported config has `CREDIT_TARGET` as a number (default 0). | Used `DYNAMIC_TARGET = !cfg.CREDIT_TARGET` (falsy-0 test). Equivalent at the default and for any explicit non-zero target; differs only for the meaningless `CREDIT_TARGET=0` explicit env, which we treat as "dynamic". Noted for Wave 6. **W6 resolved — preserved with rationale:** `CREDIT_TARGET=0` is a meaningless stop-target (a credit goal of zero would stop instantly), so collapsing it into "dynamic" is correct; the falsy-0 test is identical to the legacy string-presence test for the default and every sensible non-zero target. Kept; pinned by config parity tests. | resolved |
| 24 | W3 | bot2.mjs `frame.symbol !== 'FRAME_PROBE'` fleet filter | Legacy excludes probes from the trading fleet by frame symbol. The shared `Ship.frame` type has no `symbol` field. | Dropped the frame-symbol check; probes are excluded structurally by `cargo.capacity > 0` (probes carry no cargo) in `main.ts`. Behaviour-equivalent for the trade fleet. Also: `gateCreditOk` is now synchronous and no longer calls `reloadGateLevers` inline (legacy did); the lever poll moved to the `targetWatch` cadence. **W6 resolved — preserved with rationale:** the shared `Ship.frame` type has no `symbol` field, and every probe has `cargo.capacity === 0`, so the structural filter excludes exactly the same ships as the legacy `frame.symbol !== 'FRAME_PROBE'` test for trade-fleet selection. Kept. | resolved |
| 25 | W3-merge | eslint.config.js (root) | Root `pnpm lint` (`eslint packages/*/src`) was red (425 errors): the glob swept in the generated Prisma client (`packages/api/src/generated/**` — `index.d.ts` is ~455KB of `any`/empty-object types), fastify async plugins legitimately tripped `require-await`, and the test files (excluded from the package tsconfigs) couldn't be resolved by the type-checked project service (2 parsing errors). Sub-sessions only ran package-scoped lint so never saw it. | Added `**/generated/**` to eslint ignores; scoped `require-await: off` to `packages/api/src/{app.ts,routes,plugins}` (fastify async-plugin contract); applied `tseslint.configs.disableTypeChecked` to test files (resolves both their type-aware noise and the project-service parsing error). Fixed one real `consistent-type-imports`/`no-import-type-side-effects` issue in `app.ts`. `pnpm lint` now exits 0. | resolved |
| 26 | W4 | bot2.mjs L2640-2693 (worker TRADE_FIRST peek) | When `TRADE_FIRST=1` the worker peeks a lane via `claimLane(ship, buildLanes(...), markets)` (`lanePref`), then the normal section calls `claimLane` again. `claimLane` is **side-effecting**: it locks the good (`gs(sym).lockedBy`) and `commit()`s the lane cost. Because the re-claim skips ALL locked goods (incl. the one just locked), the second call picks a DIFFERENT lane, permanently leaking the first lane's lock + committed cash (never `uncommit`ed). `TRADE_FIRST` defaults OFF so it rarely fires. | Ported verbatim in `contracts/contracts.ts` `createContractHooks` (peek `claimLane` then fall through to TRADE). Preserve; fix in W6 by making the peek non-mutating or threading the pre-claim into the trade section. **W6 resolved — FIX APPLIED (behavioural, regression-tested):** factored `claimLane` in `trade/lanes.ts` into a pure `selectLane` (scoring + park decision, **no mutation**) + a mutating `claimLane` (locks the good + `commit()`s only on a real lane) + a non-mutating `peekLane`. The contracts hook now uses `peekLane`, so the worker's single real `claimLane` in the trade section is the only side-effecting call → no leaked lock/commit. **Also restored the legacy `lanePref` skip-contract gate** that the W4 port had silently dropped: when `TRADE_FIRST` and a lane (or park sentinel) is available the hook returns false to skip the contract leg, matching `bot2.mjs:L2640-2693`. Proven by new tests: `lanes.test.ts` (peek is non-mutating, no good locked, `committed` unchanged) + `contracts.test.ts` (TRADE_FIRST skips the contract when a lane is available). | resolved |
| 27 | W4 | bot2.mjs L274-281 vs L2478 `deliverOrphanGateCargo` | The header comment lists the rescue priority as (1) SELF fuel-aware multi-hop, (2) TRANSFER to co-located hauler, (3) stage-hop — but the spec (wave-4 §4.1) and the body interleave a distinct "SELF + fuel-cargo" tier, giving a 4-tier order (SELF route → SELF+fuel-cargo → TRANSFER → stage-hop). The legacy comment and code are inconsistent about whether SELF-with-fuel-cargo is its own tier. | Implemented the spec's explicit 4-tier order in `gate/orphan.ts`. Behaviourally a superset of the legacy comment; no rescue path dropped. Reconcile the comment vs code in W6. **W6 resolved — FIX APPLIED (comment-only):** rewrote the `deliverOrphanGateCargo` header comment so it states the implemented 4-tier order (SELF route → SELF + fuel-cargo → TRANSFER → stage-hop), matching the body. No behavioural change. | resolved |
| 28 | W4 | bot2.mjs L1663 `DEPLETION_MODS` | `const DEPLETION_MODS = new Set(['STRIPPED','CRITICAL_LIMIT'])` is declared but **never referenced** in legacy — the migrate/candidate checks test `mods.includes('STRIPPED')` / `'CRITICAL_LIMIT'` membership inline instead. Dead code. | Sub-agents faithfully reproduced the unused const; it tripped the stricter root `no-unused-vars` lint. Removed the dead const in `mining/mining.ts` + `mining/expandMine.ts` (the inline member checks are unchanged → behaviour-identical). | resolved |
| 29 | W5 | expansion.mjs `stepOutpost` OUTPROBE arc math | `partitionMarkets` is **not** a named legacy function — it's inline arc math (`lo=floor(idx*L/n)`, `hi=max(floor((idx+1)*L/n), lo+1)`, `wps.slice(lo,hi)`) giving each resident probe a contiguous market arc to keep fresh. The `lo+1` floor guarantees ≥1 market/probe, so when probes `n > markets L` the trailing probes' arcs **overlap** the last market (not disjoint). | Extracted verbatim as the pure `expansion/partition.ts#partitionMarkets` (unit-tested: disjoint/balanced/full-coverage for n≤L, 1:1 at n==L, overlap at n>L). Behaviour identical. Revisit the n>L overlap in W6 only if undesired. **W6 resolved — preserved with rationale:** the n>L arc overlap is benign — the `lo+1` floor guarantees every probe still parks on ≥1 live market; overlap only occurs when probes outnumber markets, where sharing the last market is the desired "keep it fresh" behaviour. Kept verbatim; covered by the partition unit tests + the new parity harness (`partitionMarkets` parity over n=1..9). | resolved |
| 30 | W5 | expansion.mjs `setupOutposts` reserved-keys list | The legacy reserved-roster loop `listEnv()`s the key `'MINE_BATCH'` alongside real ship rosters (GATE_HAULERS, MINE_TRANSPORT, …). But `MINE_BATCH` is a numeric batch **size** (default 24), not a roster — so legacy reserves the literal `"24"`, wrongly excluding any ship whose symbol ends in `-24` from outpost crews. Latent bug; only bites with outposts enabled. | Preserved verbatim: `expansion.ts` stringifies `cfg.MINE_BATCH` into the reserved set. Fix in W6 (drop MINE_BATCH from the roster list). **W6 resolved — FIX APPLIED:** removed `MINE_BATCH` from the reserved-roster set in `expansion/expansion.ts` (it is a numeric batch size, not a ship roster). Outpost crews are no longer wrongly excluding ships whose symbol ends in `-24`; only the real ship-roster env keys (GATE_HAULERS, MINE_TRANSPORT, …) are reserved. | resolved |
| 31 | W5 | expansion.mjs "residency pinning" | The docs (08-roadmap) describe a `residency.json` persisted file, but `expansion.mjs` has **no such file** — real residency is **in-memory position-aware adoption**: `setupOutposts` pass-1 adopts ships already sitting in an outpost system, so a restart re-derives crews from live ship positions (restart-safe, no state file). | No fs/DB residency state ported (honours the bot's no-`fs` rule). The adoption pass is reproduced faithfully in `expansion.ts`. No `@st/api`/persistence addition needed. | resolved |
| 32 | W5 | expansion.mjs feature flags | The spec mentioned a `MULTI_SYSTEM` gate flag, but **no such flag exists** in legacy — expansion self-gates purely on `AUTO_EXPAND` (+ `EXPAND_AUTOBUY` for fleet auto-buy), and `maybeTrigger` additionally requires the home gate BUILT. | Did not invent `MULTI_SYSTEM`. `createExpansion` is only constructed when `cfg.AUTO_EXPAND`; the worker hook + `maybeTrigger` self-gate. All `EXPAND_*` flags added additively to `@st/shared` with bot2 code defaults (inert OFF). | resolved |
| 33 | W5 | bot2.mjs main L3180–3196 expansion dep object | The dep object passed to the expansion subsystem includes `getShipyards`, but `expansion.mjs` destructures it and **never uses it** (it has its own `shipyardsIn` via `api`). Vestigial. | `ExpansionCtx.getShipyards` is declared optional and left unwired by `main.ts` (parity-faithful but marked vestigial). The real shipyard lookup uses `ctx.api`. Drop the dep in W6. **W6 resolved — FIX APPLIED:** removed the vestigial `getShipyards` field from `ExpansionCtx` (`expansion/expansion.ts`) and its unwired mention in `main.ts`. Expansion's shipyard lookup uses `ctx.api`/`shipyardsIn` exclusively; build + typecheck clean after removal. | resolved |
| 34 | W5 | Docker/compose (Wave-0/1 skeleton) | Bringing up the full stack surfaced several pre-existing infra bugs that blocked `docker compose up`: (a) bot `Dockerfile` CMD ran `dist/index.js` (re-export only, never calls `main`); (b) compose `bot` used `ST_TOKEN` (bot reads `SPACETRADERS_PLAYER_AGENT_TOKEN`) and sat behind `profiles: full`; (c) api `Dockerfile` `COPY coords.csv` but the seed file was absent from the monorepo; (d) `.gitignore` `!coords.csv` negation had an **inline comment**, so git still ignored it; (e) api runtime `--prod` install omitted the `prisma` CLI the migrate CMD needs, and the CLI bin lives under `packages/api/node_modules/.bin` (pnpm workspace), not the root; (f) `node:20-alpine` lacks OpenSSL for Prisma's engine; (g) the **host-generated** Prisma client (darwin engine) leaked into the image (no `.dockerignore`), crashing the server; (h) the api healthcheck used `localhost` → IPv6 `::1` (no listener) inside the container. | Fixed all in W5 (in-scope to make the stack boot): bot CMD→`dist/main.js`; compose `bot` env renamed + `env_file` (`deploy/bot.env`, optional) + `DRY_RUN` default 1 + `restart` + removed `profiles`; ported `coords.csv` to repo root + fixed the `.gitignore` negation; api runtime full install + `packages/api/node_modules/.bin/prisma` path; `apk add openssl`; added `.dockerignore` excluding `**/src/generated` + pinned `binaryTargets=["native","linux-musl-arm64-openssl-3.0.x","linux-musl-openssl-3.0.x"]`; healthcheck `localhost`→`127.0.0.1`. Stack now boots postgres→api(healthy)→bot. | resolved |
| 35 | W5 | DRY_RUN smoke seam (new, no legacy analogue) | The spec's offline smoke capability has no legacy equivalent (bot2 always hits the live game API). | Added `clients/dryRun.ts#createDryRunClient` (zero `fetch`, canned reads, no-op mutations) swapped in `main.ts` when `cfg.DRY_RUN`; the bot derives phase+lanes from the persistence snapshot, logs them, writes one `StatusSnapshot`, then idles on the stop watcher. New `DRY_RUN`/`DRY_RUN_CREDITS` flags (default OFF) in `@st/shared`; the live path is byte-for-byte unchanged. | resolved |
| 36 | W6 | `status.ts` `writeStatus` data block vs bot2.mjs:L2786 | The TS `data.mineFeed` block omitted two fields the legacy `bot-status.json` emits: `good: MINE_GOOD \|\| 'auto'` and `transport: [...MINE_TRANSPORT]`. This broke the field-for-field snapshot-shape parity that lets the existing monitors (dashboard/contracts/status `.mjs`) migrate to the API unchanged. | **W6 resolved — FIX APPLIED:** added `good: cfg.MINE_GOOD \|\| 'auto'` and `transport: [...cfg.MINE_TRANSPORT]` to the `mineFeed` block in `packages/bot/src/status.ts`, matching the legacy field order (enabled, feeders, busy, good, site, refiner, transport). Pinned by the new `__tests__/status-shape.test.ts` (asserts the snapshot key set == the legacy `bot-status.json` shape, top-level + gate/inputFeed/mineFeed/ship blocks). | resolved |
